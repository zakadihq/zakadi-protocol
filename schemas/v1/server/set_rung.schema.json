{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://zakadi.dev/schemas/v1/server/set_rung.schema.json",
  "title": "zakadi.v1 server-to-client message `set_rung`",
  "description": "Server-driven rung change; the client must answer with rung.",
  "type": "object",
  "properties": {
    "t": {
      "const": "set_rung"
    },
    "id": {
      "$ref": "https://zakadi.dev/schemas/v1/common.schema.json#/$defs/id"
    },
    "rung": {
      "$ref": "https://zakadi.dev/schemas/v1/common.schema.json#/$defs/rung"
    },
    "reason": {
      "enum": [
        "headroom",
        "server_load"
      ]
    }
  },
  "required": [
    "t",
    "rung",
    "reason"
  ]
}
